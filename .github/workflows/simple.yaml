
name: Test Workflow

on: push

jobs: 
  run-shell-command: 
    runs-on: ubuntu-latest
    steps:
      - name: echo a string
        run: echo "Hello world"
      - name: Check versions
        run: |
          git --version
          R --version
          python3 --version
      - name: Python commands
        run: |
          import platform
          print(platform.processor())
        shell: python






